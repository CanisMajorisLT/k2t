let React = require("react");

let WordsStatistics = module.exports = React.createClass({
    render(){
        console.log("Words statistics: ", this.props.data);

        return (
            <div></div>
        )
    }
});